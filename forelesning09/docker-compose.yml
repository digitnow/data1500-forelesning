services:
  postgres:
    image: postgres:15-alpine
    container_name: data1500-forel09
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin123
      POSTGRES_DB: forel09
      PGDATA: /var/lib/postgresql/data/pgdata
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./init-scripts:/docker-entrypoint-initdb.d
      # Test scripts (NEW)
      - ./test-scripts:/test-scripts:ro
    networks:
      - data1500-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U admin -d data1500_db"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
    driver: local

networks:
  data1500-network:
    driver: bridge
